const oauthApp = angular.module('oauthApp', []);

const HOST = `http://localhost:8080`;

oauthApp.controller('oauthCtrl', ['$scope', '$http', ($scope, $http) => {

  console.log({ $scope, $http }, this);
  
  

}]);